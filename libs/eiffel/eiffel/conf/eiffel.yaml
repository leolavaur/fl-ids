# Eiffel configuration file with Hydra

defaults:
  - _self_
  - dataset: 
  - optional src: config


hydra:
  # TODO: add overrides in the output path
  run:
    dir: ${gitdir:}/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

  sweep:
    dir: ${gitdir:}/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}